type Query {
  family: Family
  timelinesDoneToday: [Timeline]
  kazisRemainedToday: [Kazi]
}

type Category {
  id: String
  name: String
}

type Family {
  id: String
  name: String
  users: [User]
}

type Kazi {
  id: String
  familyId: String
  family: Family
  categoryId: String
  category: Category
  name: String
  point: Int
  repeat: Repeat
}

type Repeat {
  type: String
  activatedAt: String
}

type Timeline {
  id: String
  kazi: Kazi
  user: User
  doneAt: String
  memo: String
}

type User {
  uid: String
  name: String
  family: Family
}
